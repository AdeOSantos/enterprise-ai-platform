server:
  port: 8080

spring:
  application:
    name: ai-inference-service

model:
  registry:
    url: http://mlflow:5000
  artifact:
    path: /models/artifacts

logging:
  level:
    com.company.ai: INFO
